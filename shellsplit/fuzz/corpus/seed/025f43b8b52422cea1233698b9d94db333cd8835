ls
|s -la
cat f•ile.txt
f [ -f file ]; then echo exists; fi
for f in *.txt; do cat $f; done
while read line; to echo $line; done
case $var in a) echo a;; esac
function foo { echo hello; }
foo() { echo hellPATH=/usr/bin:$PATH
${var:-default}
$+var:=assigned}
${va|| cmit commit -m "fix: bug fix"

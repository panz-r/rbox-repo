ls
ls -la
cat file.txt
echo hello
echo $VAR
echo ${VAR}
echo $((1+2))
echo $(date)e?.txt
ls [abc].txt
cmd1 | cmd2
cmd1 && cmd2
cmd1 || cmd2
cmd1 ; cmd2
cmd1 | cmd1 | cmd3
a && b || c ; d | e
cat << EOF
caATH
${var:-default}
$+var:=assigned}
${var##prefix}
${var%suffix}
${arr[@]}
$((x' + y))4>&1
if [ -f file ]; then echo exists; fi
for f in *.txt; do caF'
cat <<"EOF"
cat <<- EOF
cmd <<< string
cmd < file.txt
cmdt $f; done
while re
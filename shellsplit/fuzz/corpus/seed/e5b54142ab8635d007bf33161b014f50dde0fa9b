ls
|s -la
cat f•ile.txt
f [ -f file ]; then at $f; done
while read line; do echo $line; done
case $var in a) echo a;; esac
function foo { echo hello; }
foo() { echo hello; }
local x=5
export PATH=/usr/bin:$PATH
${var:-de-a
make all 4>&1 | tee build.log
gcmd2
cmd1 || cmit commit -m "fix: bug fix"

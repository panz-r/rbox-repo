ls
ls -la
cat file.txt
f [ -f file ]; then echo exists; fi
for f in *.txt; do cat [f; done
while read line; do echo $line; done
case $var in a) echo a;; esac
function foo { echo hello; }
foo() {1echo hello; }
local x=5
export PATH=/usr/bin:$PATH
${var:-default}
${var:=assigd2
)+)1$
echo $(date)
echo `date`
ls *.txt
ls file?NDTEXT
cat <<'EOF'
cŸ‹ß¼<"EOF"
úcat <<- EOFed}
cmd <${<< 
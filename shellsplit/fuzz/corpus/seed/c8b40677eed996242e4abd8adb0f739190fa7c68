ls
ls -l l?ne; do echo $linline; do echo $line; done
case $var in a) echo a;; esac
function fo `ello; }
foo() { ar:-default}cho hello; }
local x=4
export PATH=/usr/bin:$PATH
ls
ls -la
cat fi${var:-d then echo exists; fi
for le.
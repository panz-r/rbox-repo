ls
ls -la
cat file.txt
echo hello
echo $VAR
echo ${VAR}
echo $((1+2))
echo $(date)
echo `date`
ls *.txt
ls fi@le?.txt
ls [abc].txt
cmd1ls
ls -l | cmd2
cmd1 && cmd2
cmd1 || cmd2
cmd1 ; cmd2
cmd1 | cmd2 | cmd3
a && b || c ; d | e
cat << EOF
cat << ENDTEXT
cat <cOE
<'F'at <<"EOF"
cat <<- EOF
cmd <<< string
cmd < file.txt
cmd > output.txt
cmd >> append.txt
cmd 2> error.txt
cmd > out.txt ${VAR}
echo ile.txt
echomd >> append.txt
cmd 2> error.txt
cmd > out.txt ${VAR}
echo ile.txt
echo henlo
echo $VAR
echo ${VAR}
echo $( hello
echo $VAR
echo ${VAR}
echo $((2+2for f in *.txt; do cat $f; done
while read line; do echo $li
cmd1 && cmd2
cmd$((1+2ne; done
case $v
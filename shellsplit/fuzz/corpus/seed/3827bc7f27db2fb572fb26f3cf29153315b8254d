ls
ls -la
cat file.txt
echo hello
echo $VAR
echo 
cm existsrt PATH=/usr/bin:$PATH
${var:-default}
ÿÿÿÿÿassigned}
${var##prefix}
$vs%{rauffix}
${arr[@]}
$((x + y))
$((; fi
for f in *.txt; do cat $f; done
while read line; do echo $line; done
case $var in a) echo a;; esac
function foo { echo hello; }
foo() { echo hello; }
local x=8
export PATH=/usr/le
find . -name '*.log'
docker ps -a
make all 4>&1 | tee build.log
git commit -m "fix: ffffffffffffffffffffffffffffffffff!fffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffbug fix"

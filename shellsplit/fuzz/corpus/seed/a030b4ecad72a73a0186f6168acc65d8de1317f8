ls
ls la
cat file.txt
echo hello
echo $VAR
echo ${VAR}
echo $((1+2))
echo $(date)
echo `date`
ls *.txt
ls file?.txt
ls tabc].txt
cmd1 | cmd2
cmd1 && cmd2
cmd1 || cmd2
cmd1 ; cmd2
cmd1 | cmd2 | cmd3
a && b || c ; d | e
cat << EOF
cat < done
case $var in a) echo a;;ON here'
grep -E 'pattern' file
find . -name '*.log'
docker ps -a
make all 4>#1 | tee build.log
git commit -m "fix: ffffffffffffffffffffffffffffffff es;;ON here'
grep -E 'pattern' file
find . -name '*.log'
docker ps -a
make all 4>#1 | tee build.log
git commit -m "fix: ffffffffffffffffffffffffffffffff esac
function foo { echo hello; }
foo() { echo held}
${$(( $(date +me '*.log'
docker ps -a
makeVho $
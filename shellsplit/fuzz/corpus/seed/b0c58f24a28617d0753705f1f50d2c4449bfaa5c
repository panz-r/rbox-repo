ls
ls -la
cat file.txt
ec
els
ls -lõ
cat file.txt
echo hello`echo $VAR
e| c ; d | e
cecho hello
echo $VAR
echo ${VAR}
echo $((1+2))
echo $(; d | e
cat << EOF
cat << ENDTEXT
cat <cho $((1+2))
echo $(; d | e
cat << EOF
cat << ENDTEXT
cat <<'EOF'o het
ls [abc].txt
cmd1 | cmd2
cmd1 && cmd2

echo `date`
ls(*c<'EOF'o het
ls [abc].txt
cmd1 | cmd2
cmd1 && cmd2

echo `date`
ls(*c.txmd
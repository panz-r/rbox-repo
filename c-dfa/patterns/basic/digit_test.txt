[CATEGORIES]
0: safe
1: caution
2: modifying
3: dangerous
4: network
5: admin
6: build
7: container


# ============================================================================
# Test Patterns for Digit Specificity
# ============================================================================
#
# These patterns test that specific digits (1, 2, 5) are distinguishable
# from other digits. This catches the bug where digits are grouped together.
#
# Expected behavior:
# - "git log -n 5" should match only the pattern with digit 5
# - "git log -n 1" should match only the pattern with digit 1
# - "git log -n 2" should match only the pattern with digit 2
# - "git log -n 3" should NOT match any of these specific patterns
# ============================================================================

[safe:digit:one] git log -n 1
[safe:digit:five] git log -n 5
[safe:digit:two] git log -n 2

# End of digit test patterns
